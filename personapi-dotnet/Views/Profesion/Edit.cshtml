@model personapi_dotne.Models.Entities.Profesion

@{
    ViewData["Title"] = "Editar Profesión";
}

<section style="background:#f0fdf4; padding:2rem; border-radius:1rem; max-width:800px; margin:auto;">
    <h2 style="text-align:center; color:#198754;">🧩 Editar Profesión</h2>

    <form asp-action="Edit" style="display:flex; flex-direction:column; gap:1rem;">
        <input type="hidden" asp-for="Id" />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div>
            <label asp-for="Nom" style="font-weight:600;">Nombre:</label><br>
            <input asp-for="Nom" style="width:100%; padding:0.7rem; border:1px solid #bbb; border-radius:0.5rem;">
            <span asp-validation-for="Nom" class="text-danger"></span>
        </div>

        <div>
            <label asp-for="Des" style="font-weight:600;">Descripción:</label><br>
            <textarea asp-for="Des" rows="4" style="width:100%; padding:0.7rem; border:1px solid #bbb; border-radius:0.5rem;"></textarea>
            <span asp-validation-for="Des" class="text-danger"></span>
        </div>

        <div style="display:flex; justify-content:space-between;">
            <button type="submit" style="background-color:#198754; color:white; border:none; padding:0.6rem 1.2rem; border-radius:0.5rem;">💾 Guardar Cambios</button>
            <a asp-action="Index" style="background-color:#6c757d; color:white; text-decoration:none; padding:0.6rem 1.2rem; border-radius:0.5rem;">↩️ Cancelar</a>
        </div>
    </form>
</section>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
