@model personapi_dotne.Models.Entities.Profesion

@{
    ViewData["Title"] = "Agregar Profesión";
}

<section style="background-color:#f9fafb; padding:2rem; border-radius:1rem; box-shadow:0 0 10px rgba(0,0,0,0.1); max-width:800px; margin:auto;">
    <h2 style="color:#333; text-align:center; margin-bottom:1.5rem;">🛠️ Nueva Profesión</h2>

    <form asp-action="Create" style="display:flex; flex-direction:column; gap:1.2rem;">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div>
            <label asp-for="Nom" style="font-weight:600;">Nombre:</label><br>
            <input asp-for="Nom" style="width:100%; padding:0.7rem; border:1px solid #ccc; border-radius:0.5rem;">
            <span asp-validation-for="Nom" class="text-danger"></span>
        </div>

        <div>
            <label asp-for="Des" style="font-weight:600;">Descripción:</label><br>
            <textarea asp-for="Des" rows="5" style="width:100%; padding:0.7rem; border:1px solid #ccc; border-radius:0.5rem;"></textarea>
            <span asp-validation-for="Des" class="text-danger"></span>
        </div>

        <div style="display:flex; justify-content:space-between;">
            <button type="submit" style="background-color:#0d6efd; color:white; border:none; padding:0.6rem 1.2rem; border-radius:0.5rem; cursor:pointer;">💾 Guardar</button>
            <a asp-action="Index" style="background-color:#6c757d; color:white; text-decoration:none; padding:0.6rem 1.2rem; border-radius:0.5rem;">↩️ Volver</a>
        </div>
    </form>
</section>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
